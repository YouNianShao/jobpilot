## 变更说明
（这个 PR 做了什么、为什么这么做）

## 关联 Issue
（如 `Fixes #123`）

## 提交前检查清单
- [ ] `git status` 干净，无敏感文件混入（config.yaml / data/ / chrome-profile/ / *.log 均被 .gitignore 拦截）
- [ ] 本地已自测功能正常
- [ ] 若改动配置项，已同步更新 `config.example.yaml`
- [ ] 文档（README.md / GIT_WORKFLOW.md）已同步更新
